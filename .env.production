# Loaded by `vite build` (production mode). Vite bakes this value into the
# bundle, and it OVERRIDES .env.local in production mode — so `npm run build`
# always points the deployed site at the real backend, not localhost.
VITE_API_BASE_URL=https://56-10-97-170.sslip.io
